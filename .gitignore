# Secrets
.env
.env.local
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
*.egg

# Pipeline outputs (generated appeal letters, verification reports)
output/

# Note: fixtures/case_*/ are committed so the recorded cassettes
# (tests/cassettes/) can replay deterministically on a fresh clone.
# `make fixtures` regenerates them in-place; if the PDFs change, the
# request hashes change and the cassettes have to be re-recorded too.

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Logs
*.log
logs/

# Node (for future UI phase)
node_modules/
.next/
.logs/
